# Local backend defaults (SQLite)
#
# Copy to `backend/.env` and adjust as needed.

DATABASE_URL=sqlite:///./docfoundry.db
CHROMA_DIR=./chroma_db
# Disable Chroma telemetry (recommended for local dev).
ANONYMIZED_TELEMETRY=False
LLM_PROVIDER=stub
# To use Cerebras (OpenAI-compatible): set `LLM_PROVIDER=cerebras` and provide `CEREBRAS_API_KEY`.
# CEREBRAS_API_KEY=
# CEREBRAS_MODEL=llama3.1-8b-instruct

# Dev JWT (for /auth + /chat)
JWT_SECRET=dev-secret-change-me
JWT_EXP_MINUTES=120

# Keep HuggingFace/Transformers caches inside the repo (avoids writing to ~/.cache in sandboxed envs)
HF_HOME=./.hf
TRANSFORMERS_CACHE=./.hf/transformers

# Optional: sentence-transformers model name; if unavailable, a deterministic hash embedder is used.
# Set `EMBED_PROVIDER=hash` to avoid model downloads.
EMBED_PROVIDER=auto
EMBED_MODEL=all-MiniLM-L6-v2
EMBED_DIM=384
